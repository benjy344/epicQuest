<%= form_with(model: donjon, local: true) do |form| %>
  <% if donjon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donjon.errors.count, "error") %> prohibited this donjon from being saved:</h2>

      <ul>
      <% donjon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :donjon_name %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.text_field :level, id: :donjon_level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
